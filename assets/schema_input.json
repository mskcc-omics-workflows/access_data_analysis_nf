{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/accessanalysis/master/assets/schema_input.json",
    "title": "nf-core/accessanalysis pipeline - params.input schema",
    "description": "Schema for the id_mappings.csv provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "cmo_patient_id": {
                "type": "string",
                "pattern": "^$|^C-[A-Z0-9]+$",
                "errorMessage": "cmo_patient_id must be empty or follow the format 'C-XXXXXX'"
            },
            "dmp_patient_id": {
                "type": "string",
                "pattern": "^$|^P-[A-Z0-9]+$",
                "errorMessage": "dmp_patient_id must be empty or follow the format 'P-XXXXXX'"
            },
            "sex": {
                "type": "string",
                "pattern": "^[MF]$",
                "errorMessage": "sex must be M or F"
            }
        }
    }
}
